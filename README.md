# nhatro-be

NestJS backend scaffold for the nhatro project.

Quick start

1. Change to the folder:

```bash
cd nhatro-be
```

2. Install dependencies (run in your environment):

```bash
npm install
# or pnpm install / yarn install
```

3. Start dev server:

```bash
npm run start:dev
```
