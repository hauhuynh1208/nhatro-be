# Application
NODE_ENV=development
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_NAME=nhatro
DB_SSL=false

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=30d
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRES_IN=45d

# Initial admin account (used by seed:admin script — developer fills these before running pnpm seed:admin)
ADMIN_USERNAME=
ADMIN_PASSWORD=
